## Environment
IDL is located at `/Applications/NV5/idl/bin/idl`.  However, unless otherwise indicated, IDL should be run without a starup file by using this string "env -i HOME=$HOME PATH=$PATH /Applications/NV5/idl/bin/idl".
